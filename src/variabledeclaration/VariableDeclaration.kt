package variabledeclaration

/**
 * dataclasses.functions.functions.functions.lambdas.functions.controlflow.variabledeclaration.interoperability.npe.npe.oop.oop.returnmultiple.main method
 * can be declared without a wrapping class
 *
 */
fun main(args: Array<String>) {
    var x:Int
    val y: Int
    x = 10
    y = 11
    println("Value x $x")
    println("Modified value x ${x * 10}")

//    y = 18
    println("Modified value y ${y * 10}")

    if (x ==10) println("Equals to 10") else print("Not equals to 10")

    val min = if (x < y) x else y
    println("Min value = $min")


    println(u)
}

/**
 * Variables Functions
 * can be declared in the top-level / root level
 */
val u = 170